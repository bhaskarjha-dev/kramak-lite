# Inbox

> Communication channel between humans and the pipeline.
> Add items to **Unprocessed** — the planner processes them at the start of each planning session.
> Processed items are moved to **Processed** with action notes.

---

## Unprocessed

<!-- Add items here with a type tag. The planner processes these first.

### [type]: [Title]
[Description of what's needed and why]

Types:
- `bug` — Bug report (security/build-blocking → immediate WI; otherwise deferred to ITERATE)
- `direction` — Priority change, pivot, new strategy
- `insight` — Research finding, user feedback, market data
- `data` — Raw data to integrate into project docs
- `credential` — API key, token, or access provided (marks corresponding human task resolved)

Example:
### direction: Add dark mode support
Users are requesting dark mode. Consider this for the next planning batch.
-->

---

## Processed

<!-- The planner moves items here after processing, with a note on what action was taken.

Example:
### direction: Add dark mode support — ✅ WI-201 created
Processed in Batch 02. Created WI-201 (Directed) for theme system implementation.
-->
